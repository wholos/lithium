# lithium
**dependencies**
``` bash
gcc
wget
libconfig
make (optional)
just (optional
```

how to build (with make)?
``` bash
make all
mv lithium /bin/
```

how to build (with just)?
``` bash
just all
mv lithium /bin/
```
